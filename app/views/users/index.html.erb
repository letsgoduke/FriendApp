test

<table>
<tr>
<td><strong>Name</strong></td>
<td><strong>Email</strong></td>
<td><strong>Email</strong></td>
</tr>

<%= @users.each do |u| %>
	<tr>
	<td><%= print u.name %></td>
	<td><%= print u.email %></td>
	<td><%= link_to "Show", u %></td>
	</tr>
<% end %>

</table>

<br/>

<br /><%= link_to "Users", users_path %>
<br /><%= link_to "Sign In", new_user_session_path %>
<br /><%= link_to "Sign Up", new_user_registration_path %>