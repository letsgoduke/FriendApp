<table>
<tr>
<td><strong>Name</strong></td>
</tr>

<% @users.each do |u| %>
	<tr>
	<td><%= print u.name %></td>
	<td><%= link_to "Show", u %></td>
	<td><%= link_to "Most Recent Post", u.post.last %></td>
	</tr>
<% end %>

</table>

<br/>

<%= link_to "Back", action: "index" %>